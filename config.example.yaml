bt:
  # When true, the BT UI is shown and the API proxies Transmission RPC.
  # Download dir, peer port, and rate/ratio limits are read from Transmission.
  enable: false
  transmission:
    url: https://bt.imoing.com/transmission/rpc
    # username: tr
    # password: ${TRANSMISSION_RPC_PASSWORD}
  # block:
  #   clients: []
  #   peer_ids: []
  #   ports: []
  #   networks: []

storage:
  backends:
    - name: local
      type: local
      config:
        root: downloads
    # - name: nas
    #   type: smb
    #   config:
    #     host: 192.168.1.10
    #     share: media
    #     username: gateway
    #     domain: ""
    #     port: 445
    #   secret: ${SMB_PASSWORD}
    # - name: archive
    #   type: s3
    #   config:
    #     endpoint: https://s3.example.com
    #     region: auto
    #     bucket: archive
    #     prefix: ""
    #     access_key_id: ${S3_ACCESS_KEY}
    #     force_path_style: true
    #   secret: ${S3_SECRET_KEY}
  # Scheduled incremental sync (cron). Copies when size differs or source mtime is newer.
  # sync:
  #   - interval: "0 */6 * * *"
  #     src:
  #       name: local
  #       path: movies
  #     dst:
  #       name: nas
  #       path: media/movies
  #   - interval: "30 2 * * *"
  #     enabled: true
  #     src:
  #       name: local
  #       path: ""
  #     dst:
  #       name: archive
  #       path: gateway-backup

dns:
  cloudflare:
    # token: ${CF_API_TOKEN}
    zones: []
    # zones:
    #   - example.com
