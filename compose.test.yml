services:
  tests:
    build:
      context: .
      dockerfile: Dockerfile.dev
      target: development
    command:
      [
        "sh",
        "-c",
        "go test ./... -count=1 && npm --prefix web run build",
      ]
